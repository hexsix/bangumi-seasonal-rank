---
const response = await fetch(
    "https://api.rinshankaiho.fun/api/v0/season/available",
).then((res) => res.json());

const current_season_id = response.current_season_id;

return Astro.rewrite(`/${current_season_id}/`);
---
